This example use following microk8s addons.
- dns
- hostpath-storage
- ingress
- storage

`microk8s.kubectl apply -k microk8s/` will set up.

wikibase => http://localhost/

SPARQL => http://localhost/q/

